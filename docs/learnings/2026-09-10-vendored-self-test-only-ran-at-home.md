ts: 2026-09-10T04:47:20Z
commit: d4f8dbf
session: claude-code meridian-datum-adoption (https://claude.ai/code/session_01X6SuqCyyde9KbvvmANfCnK)
status: verified
fact: The conformance pack shipped, with green CI, in a state where its own self-test could not run once vendored: `conformance/test.mjs` built its PIN exercise by copying the row schema from `../schema/gate-verdict.v1.json`, a path that exists only inside this repo. In a vendored copy, where the contract puts the schema beside `check.mjs`, the self-test died with `ENOENT` before asserting anything. CI never saw it because CI only ever runs the pack at home, which is precisely the arrangement that cannot detect this class of defect. `check.mjs` had the two-location lookup from the start; `test.mjs` did not, so the checker was portable and the thing that proves the checker works was not. The rule this yields: a pack that is meant to be vendored has to have its self-test exercised from a copy that is NOT the source tree, and CI must run it that way, or "the self-test is green" is a claim about one directory layout.
basis: At `d4f8dbf` (the fix), from a copy that mimics the vendoring layout: `d=$(mktemp -d) && cp -r conformance/. "$d" && cp schema/gate-verdict.v1.json "$d/" && node "$d/test.mjs" --mutate` -> `ok conformance: 13 positive, 46 negative, 0 real, 17 reasons, 13 rules mutated`, exit 0. Negative control from the parent commit, same layout: `d2=$(mktemp -d) && git archive 48dc48c conformance | tar -x -C "$d2" && cp schema/gate-verdict.v1.json "$d2/conformance/" && node "$d2/conformance/test.mjs" --mutate` -> `Error: ENOENT: no such file or directory, lstat '<tmp>\schema\gate-verdict.v1.json'`, exit 1. Found 2026-09-09 during the first adoption (meridian), when the vendored self-test crashed; both halves above were re-captured at curation against the commit named here.
re-verify: d=$(mktemp -d) && cp -r conformance/. "$d" && cp schema/gate-verdict.v1.json "$d/" && node "$d/test.mjs" --mutate
