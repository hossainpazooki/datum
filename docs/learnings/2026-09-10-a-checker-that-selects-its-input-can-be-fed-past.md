ts: 2026-09-10T04:47:43Z
commit: d4f8dbf
session: claude-code meridian-datum-adoption (https://claude.ai/code/session_01X6SuqCyyde9KbvvmANfCnK)
status: verified
fact: Two ways a bad row passed the checker, both found by an adversarial pass over a build whose self-test was already green, and both of the same shape -- the checker narrowing its own input before judging it. (1) `check.mjs` selected rows by a case-sensitive `.json` suffix, so a refusable row named `BAD.JSON` in the rows directory was never read and the directory passed clean; the same hole swallowed `.jsonl` and extensionless files, and nothing reported a skipped file. Every regular file in a rows directory is now a row, and one that does not parse as JSON makes the directory unevaluable (exit 2) rather than invisible. (2) The status-literal rule walked object VALUES only, so `params: {"CLAIMABLE": 1}` was accepted while `params: {"x": "CLAIMABLE"}` was refused; the scan now reads keys as well. Neither hole was reachable by the fixture corpus as it stood, because every fixture handed the checker a well-named file and a well-shaped object -- a corpus tests the judging, never the selecting, so the input boundary needs its own controls (both are now fixtures and CLI cases in `test.mjs`). The general rule: a gate that decides what to look at is making an unaudited claim about what does not matter.
basis: At `d4f8dbf`, a rows directory holding one conforming row as `GOOD.json` and one live-RED row as `HIDDEN.JSON` -> `FAIL HIDDEN.JSON: second live cell for example-lane1-p1:lane1 (have GOOD.json)` and `FAIL HIDDEN.JSON: live RED needs a human: a real surface failed`, exit 1 (before the fix: exit 0, `ok 3 rows conform`, CLAIMABLE). Key scan, in process: `checkRows` over a positive fixture's row with `params.CLAIMABLE` added -> `["status literal in row: \"CLAIMABLE\""]` (before the fix: `[]`). Both defects were found 2026-09-09 against the then-uncommitted pack, fixed test-first before the first push, and so never existed in this repo's committed history; the outputs above were captured at curation against the commit named here.
re-verify: d=$(mktemp -d) && node -e 'const f=require("fs"),p=process.argv[1];const c=JSON.parse(f.readFileSync("conformance/fixtures/positive/01-live-green.json","utf8"));f.writeFileSync(p+"/GOOD.json",JSON.stringify(c.row));f.writeFileSync(p+"/HIDDEN.JSON",JSON.stringify({...c.row,result:"RED",checks:{duplicate_absorbed:1,collision_refused:0}}))' "$d" && node conformance/check.mjs "$d" 2>&1 | head -1
