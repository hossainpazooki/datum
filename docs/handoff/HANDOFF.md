# Handoff index

Pointers only; evidence lives in the entries. Entries are immutable -- a later
session writes a new entry, never edits an old one.

- [2026-09-10 -- pack built and first adoption](2026-09-10-pack-built-and-first-adoption.md) --
  the conformance pack built, pushed and CI-green at `d4f8dbf`, and vendored
  once for real by meridian; `fixtures/real/` still empty, and the pin's
  write path has already produced an empty pin downstream.
